# Критичные баги и замечания в текущем проекте

Необходимо проанализировать эти кртичиные баги и устранить.
Баги рушат архитектуру проекта, мешают разработки.

## Баг сложных хэндлеров
Изначально был баг дублирования хэндлеров и контроллеров.
Видимо ты забыл (не понял идею), поэтому нагенерил много сложного кода.

Предлагаю зафиксировать такую терминологию.
Хэндлер - обрабочик только одного запроса (request, response), например,
получить пользователя по id.
Контроллер - группа хэндлеров, свазянная по смыслу (все, что связано с пользователем напряму: получить, удалить, модифицировать, создать).

Сейчас класса имею суффикс Handler, но по факту это контроллеры, в которых запихали все связанные хэндлеры.

Вначале я решил так оставить, но понял, что это быстрый путь в говно-код.
Вот почему - самый быстрый и простой ответ - нарушается принцип SOLID открытости-замкнутости OCP.

Смотрим на код на примере PortfolioHandler:
```cpp
    void handle(IRequest& req, IResponse& res) override
    {
        // Проверяем авторизацию
        auto userId = extractUserId(req);
        if (!userId) {
            res.setStatus(401);
            res.setHeader("Content-Type", "application/json");
            res.setBody(R"({"error": "Unauthorized"})");
            return;
        }

        // Получаем активный счёт
        auto account = accountService_->getActiveAccount(*userId);
        if (!account) {
            res.setStatus(400);
            res.setHeader("Content-Type", "application/json");
            res.setBody(R"({"error": "No active account found"})");
            return;
        }

        std::string path = req.getPath();
        
        if (path == "/api/v1/portfolio") {
            handleGetPortfolio(req, res, account->id);
        } else if (path == "/api/v1/portfolio/positions") {
            handleGetPositions(req, res, account->id);
        } else if (path == "/api/v1/portfolio/cash") {
            handleGetCash(req, res, account->id);
        } else {
            res.setStatus(404);
            res.setHeader("Content-Type", "application/json");
            res.setBody(R"({"error": "Not found"})");
        }
    }
```
И как регистрируется это:
```cpp
    // Portfolio Handler
    {
        auto handler = injector.create<std::shared_ptr<trading::adapters::primary::PortfolioHandler>>();
        handlers_[getHandlerKey("GET", "/api/v1/portfolio")] = handler;
        handlers_[getHandlerKey("GET", "/api/v1/portfolio/positions")] = handler;
        handlers_[getHandlerKey("GET", "/api/v1/portfolio/cash")] = handler;
        std::cout << "  ✓ PortfolioHandler: GET /api/v1/portfolio" << std::endl;
    }
```

Вместо того, чтобы создать три хэндлера PortfolioHandler, PositionHandler, CashHandler, создан один негибкий хэндлер.

Какие минусы:
1. Чтобы добавить/убрать endpoint (в нашем случае endpoint равносилен хэндлеру), нужно рефакторить код PortfolioHandler (который контроллер, тут может путаница возникнуть а терминологии, имеется ввиду текущий "контроллер").
Эти if..else ломают всю архитектуру.
2. Мы три раза сейчас регистрируем на разные ключи один handler (контроллер), а потом пытаемся в нем понять какой же ключ через if-условия, это жесткий изврат и просадка производительности + не гибко.

Как надо:
1. На каждый endpoint регистриуем свой хэндлер.
2. Никаких if-условий, теперь все автоматом из коробки происходит.
Приложение при поступлении запроса ищет в карте по ключу подходящий endpoint, а не перебирает if-условия.
3. Легко рефакторить. Если возник баг в одном endpoint-е, то правим только его.
4. Можно легко добавлять/удалять хэндлеры, не меняя код других хэндлеров.
5. Тесты проше для понимания и отдалдки.

Минус тут только один - связанные хэндлеры реализованы как отдельные классы, но зато гибко и правильно с точни зрения OCP.
Если у тебя есть другая лучшая идея - озвуч.

Надо взять один тяжелый хэндлер (контроллер) и распилить его на маленькие независимые хэндлеры (endpoint-ы), распилить тесты. Псосмотреть насколько это гибко будет.

# Баг лишних методов
Периодически нахожу лишние методы, нужно стараться избегать лишней логики, создавать только по необходимости.

Например, во многих местах есть вот такая фигня:
```cpp
    /**
     * @brief Извлекает путь без query string
     */
    std::string extractPath(const std::string& fullPath)
    {
        size_t pos = fullPath.find('?');
        if (pos != std::string::npos) {
            return fullPath.substr(0, pos);
        }
        return fullPath;
    }
```

Непонятна зачем она нужна, у нас в интерфеймсе IRequest уже есть все необходимое.
```cpp
struct IRequest {
    virtual ~IRequest() = default;

    /**
     * @brief Получить путь запроса
     */
    virtual std::string getPath() const = 0;
    /**
     * @brief Получить метод запроса (GET, POST, и т.д.)
     */
    virtual std::string getMethod() const = 0;
    /**
     * @brief Получить тело запроса
     */
    virtual std::string getBody() const = 0;

    /**
     * @brief Получить параметры запроса в виде ключ-значение
     */
    virtual std::map<std::string, std::string> getParams() const = 0;

    /**
     * @brief Получить HTTP-заголовки запроса
     */
    virtual std::map<std::string, std::string> getHeaders() const = 0;

    /**
     * @brief Получить IP-адрес (клиента при входящем сообщении, получателя при исходящем)
     */
    virtual std::string getIp() const = 0;

    /**
     * @brief Получить порт (для входящих - 80 по умолчанию, для исходящих - целевой порт)
     */
    virtual int getPort() const = 0;
};
```
Наш самописный фреймворк уже сам все извлекает и формирует структуру.
Если прям что-то реально нужно, то надо внести предложения по можернизации фреймворка, но никак не "городить огород".

# Баг дублирования данных (нарушение принципа DRY)
В нескольких местах есть дублирование информации, это может привести к проблемам в будущем. Я нашел два места, но их может быть больше. Надо проанализировать и зафиксировать места, предложить идеи по рефакторингу.

1. В классе Instrument есть поле std::string currency, а также поле Money minPriceIncrement, а в классе Money есть также поле  std::string currency.
В Instrument два раза дулируется код валюты, что может привести к непредсказуемым последствиям.
Как вариант - убрать std::string currency из Instrument, оставить только в Money.

2. В классе OrderRequest есть поле accountId и в метод broker_->placeOrder передаются два аргумента const std::string& accountId, const domain::OrderRequest& request.
Из-за этого accountId два раза повторяется. Пока не знаю как лучше сделать.
Наверно самый простой вариант - в брокер-сервисе убрать первый аргумент std::string& accountId. Тут надо подумать, рассмотреть разные идеи.

# Баг кривых endpoint-ов
Нужно еще раз пробежаться по всем endpoint-ам, котрые сейчас есть.
Создать отдельный md-файл с описанием: метод, путь, параметры, как работают под капотом, как проверять с помощью curl-а.
Взять за базу CURL_CHECKLIST.md.

Скорректировать endpoint-ы, чтоб потом меньше боли было.
Вначале задокументировать текущее состояние.

# Баг формироания отчета об уровне покрытия в Github Action
Тесты корректно "попадают" в запускаемый файл mvp_tests. Локально тесты проходят.
Но в Github Action почему-то тесты вообще не находятся. В чем причина надо разобраться.
```yaml
name: CMake CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    
    steps:
    - name: 🔄 Checkout
      uses: actions/checkout@v3
      
    - name: 🛠 Install dependencies
      run: sudo apt-get update && sudo apt-get install -y cmake g++ gcovr
      
    - name: 🏗 Configure with coverage
      run: cmake -B build -DCMAKE_BUILD_TYPE=Debug -DCMAKE_CXX_FLAGS="--coverage -g -O0"
      
    - name: ⚙️ Build
      run: cmake --build build
      
    - name: ✅ Run tests
      run: ctest --test-dir build --output-on-failure --verbose
      
    - name: 📊 Generate HTML coverage report
      run: |
        gcovr \
          --root . \
          --exclude '.*CMakeFiles/.*' \
          --exclude '.*CompilerIdCXX.*' \
          --exclude '.*CompilerIdC.*' \
          --exclude '.*cmake/.*' \
          --exclude '.*tests/.*' \
          --exclude '.*_deps/.*' \
          --exclude '.*demo/.*' \
          --exclude '.*benchmarks/.*' \
          --gcov-ignore-parse-errors=all \
          --html-details coverage.html \
          --print-summary
              
    - name: 📤 Upload coverage report
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: coverage-report-html
        path: coverage*.html
```

# Баг дублирования методов генерации UUID
В разных местах используется генерация std::string generateUuid().
Надо сделать отдельную утилиту по генерации UUID, а не изобретать велосипед каждый раз.

Проверить код на дублирование еще каких-то методов.

# Тупая реализация кэша
В MarketService ужасно реализован кэш.
Я так долго готовил библиотеку кэширования, чтоб в итоге какая-то дичь получилась.

Во-первых, смысл в убогом ICachePort нет, т.к. нужно кжшировать много разных данных, т.е. ICachePort должен быть макро-кэшем (картой кэшей), тогда смысл еще есть.

Сейчас в MarketService прям эесткий говно-код с каким-то трешем: сериализация, лесераилизация... Когда можно просто испоьзовать готовые из коробки кэши из моей библиотеки с разным типами и разными политиками TTL, тогда никаких сериализаций/десириализай не нужно. Очень глупо сейчас реализована идея макро-кэша.

Может стоит подумать на создание декоратора над MarketService: по умолчанию без кэша, а декоратор добавляет набор кэшей, то, что я описал выше. Надо подумать.