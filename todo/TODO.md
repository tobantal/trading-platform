# 🏗️ Trading Platform — Roadmap v5.0

## Курс: Microservice Architecture (OTUS)
## Дата: 16 декабря 2025

---

# 📅 Ключевые даты

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                            TIMELINE                                          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  14.12 ✅         21.12              28.12              3.01                │
│    │               │                  │                  │                  │
│    ▼               ▼                  ▼                  ▼                  │
│ HelloWorld      MVP               Education          ВИДЕО                 │
│  DONE!        дедлайн             дедлайн           защита                 │
│                                                                             │
│  ════════════════════════════════════════════════════════════              │
│  ▓▓▓▓▓▓▓▓▓▓░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░              │
│  HelloWorld │     MVP (5 дней)     │  Education (6 дней)  │               │
│    done     │                      │                      │               │
│                                                                             │
│  Сегодня: 16.12 (пн) — НАЧАЛО MVP                                          │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

# 🎯 4 этапа развития

```
┌─────────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│ HELLOWORLD  │───▶│    MVP      │───▶│  EDUCATION  │───▶│ PRODUCTION  │
│    ✅ DONE  │    │  В РАБОТЕ   │    │    ПЛАН     │    │   FUTURE    │
└─────────────┘    └─────────────┘    └─────────────┘    └─────────────┘
      │                  │                  │                  │
      ▼                  ▼                  ▼                  ▼
 Инфраструктура    Бизнес-логика     Микросервисы      Реальный API
 Docker Compose    Fake Tinkoff      Service Split     Tinkoff gRPC
 FetchContent      PostgreSQL        RabbitMQ          Kubernetes
 Prometheus        JWT Auth          API Gateway
                   SMA Strategy      ZITADEL
```

---

# ✅ Этап 1: HelloWorld (ВЫПОЛНЕН)

## Что сделано

| Компонент | Статус | Описание |
|-----------|:------:|----------|
| Docker Compose | ✅ | backend, nginx, postgres, prometheus |
| FetchContent | ✅ | cpp-http-server, cpp-cache |
| REST API | ✅ | /health, /metrics, /echo |
| Boost.DI | ✅ | Handlers через DI |
| Web UI | ✅ | index.html |

## Результат

```bash
docker compose up -d
curl http://localhost:8080/api/v1/health  # ✅ работает
curl http://localhost:8080/metrics        # ✅ работает
open http://localhost:3001                # ✅ работает
```

---

# 🚀 Этап 2: MVP (16-21 декабря)

## Цель

Рабочий продукт с гексагональной архитектурой, Fake Tinkoff API и одной торговой стратегией.

## План по дням

### День 1 (пн 16.12): Структура + Domain + Ports

**Задачи:**
```
□ Скопировать hello-world → mvp
□ Создать структуру директорий (ports/, domain/, adapters/, etc.)
□ Создать domain entities:
  └─ Money.hpp, Account.hpp, Order.hpp, Quote.hpp
  └─ Position.hpp, Strategy.hpp, Signal.hpp
  └─ Events.hpp (DomainEvent, OrderCreatedEvent, etc.)
□ Создать Input Ports:
  └─ IAuthService.hpp, IMarketService.hpp, IOrderService.hpp
  └─ IPortfolioService.hpp, IStrategyService.hpp, IAccountService.hpp
□ Создать Output Ports:
  └─ IBrokerGateway.hpp, IUserRepository.hpp, IOrderRepository.hpp
  └─ IStrategyRepository.hpp, IJwtProvider.hpp, ICachePort.hpp, IEventBus.hpp
□ Добавить ThreadSafeMap.hpp (из курса "Паттерны")
```

**Deliverable:** Компилируется, структура готова.

---

### День 2 (вт 17.12): Adapters — Secondary (Output)

**Задачи:**
```
[Done] SimpleBrokerGatewayAdapter (IBrokerGateway):
  └─ 5 инструментов (SBER, LKOH, GAZP, VTBR, YNDX)
  └─ getQuote() — базовая цена ± 5%
  └─ getPortfolio() — in-memory позиции
  └─ placeOrder() — симуляция исполнения
□ InMemoryEventBus (IEventBus):
  └─ ThreadSafeMap для subscribers
  └─ publish(), subscribe(), unsubscribe()
□ FakeJwtAdapter (IJwtProvider):
  └─ HTTP клиент к fake-jwt-server
  └─ createToken(), validateToken()
□ LruCacheAdapter (ICache):
  └─ Обёртка над cpp-cache
```

**Deliverable:** Все secondary adapters компилируются и работают автономно.

FIXME: fake-jwt-server не подключен по факту!

---

### День 3 (ср 18.12): Application Services + PostgreSQL

**Задачи:**
```
□ SQL миграции:
  └─ 001_reference_tables.sql (справочники)
  └─ 002_main_tables.sql (users, accounts, orders, strategies, signals)
□ PostgreSQL Repositories:
  └─ PostgresUserRepository
  └─ PostgresAccountRepository
  └─ PostgresOrderRepository
  └─ PostgresStrategyRepository
□ Application Services:
  └─ AuthService
  └─ MarketService
  └─ OrderService
  └─ PortfolioService
  └─ AccountService
```

**Deliverable:** Сервисы работают с БД.

FIXME: по факту пропущен этап, реализованы временно InMemory репозитории.
Репозтории все равно нужны.
---

### День 4 (чт 19.12): Controllers + Strategy

**Задачи:**
```
□ Primary Adapters (Controllers):
  └─ AuthController (POST /auth/login)
  └─ MarketController (GET /quotes, GET /instruments)
  └─ OrderController (POST /orders, DELETE /orders/{id})
  └─ PortfolioController (GET /portfolio)
  └─ StrategyController (POST/GET /strategies, POST /{id}/start)
  └─ HealthController, MetricsController (уже есть, адаптировать)
□ TradingApp:
  └─ DI конфигурация для всех сервисов
  └─ Регистрация всех routes
□ SMA Strategy:
  └─ SmaStrategy.hpp/cpp
  └─ StrategyRunner — запуск/остановка
```

**Deliverable:** Все endpoints работают.

FIXME: SMA Strategy не реализована.

FIXME: убрать дублирование (есть хэндлеры и контроллеры).
Как вариант использовать такой подход  терминологию.
Хэндлер - обрабочик только одного запроса (request, response), например,
получить пользователя по id.
Контроллер - группа хэндлеров, свазянная по смыслу (все, что связано с пользователем напряму: получить, удалить, модифицировать, создать).

Можно сильно заморочиться - создавать отдельные классы-обработчики хэндлеры, а потом в контроллеры инжектить эти хэндлеры (так можно получить гибкость, корректровать обработку одного эндлера по отдельности, но при этом иметь структуру, связыающую хэндлеры).
Пока можно не тратить время на рефакторинг, в целом работает. Но обязательно это отобразить в TODO, может к этому вернусь позже.

FIXME: инструкция по тестированию curl имеет ошибки.
Например, curl "http://localhost:8080/api/v1/quotes?figi=BBG004730N88" выдает все результаты, нужно curl "http://localhost:8080/api/v1/quotes?figis=BBG004730N88"
Необходимо проверить на ошибки все endpoint-ы и инструкцию.
Проверить на сколько запросы отвечают концепции rest (пробежаться по каждому endpoint-у, добавить комментарии к каждому endpoint-у, если еще нет).

FIXME: MetricsController еще с HelloWorld не работает, нужно будет отлельное исследование  правки произвести.

FIXME: Стоит подумать над созданием однотипных документированных unit-тестов на проверку endpoint-ов.

---

### День 5 (пт 20.12): UI + Docker + Тестирование

**Задачи:**
```
□ Web UI (html/):
  └─ Логин форма
  └─ Портфель (таблица позиций)
  └─ Котировки (таблица)
  └─ Создание ордера
  └─ Список стратегий
□ Docker Compose:
  └─ Добавить fake-jwt-server
  └─ Добавить volume для миграций
  └─ Health checks для всех сервисов
□ Тестирование:
  └─ curl скрипты (demo.sh)
  └─ Проверка всех сценариев
□ Документация:
  └─ README.md для MVP
```

**Deliverable:** `docker compose up -d` → всё работает.

---

### День 6 (сб 21.12): Буфер + Финализация

**Задачи:**
```
□ Исправление багов
□ Рефакторинг (если нужно)
□ Дополнительные тесты
□ C4 диаграммы (если успеваем)
```

**Deliverable:** MVP готов к демонстрации.

---

## MVP Checklist

### Функциональные требования

| # | Требование | Приоритет | Статус |
|---|------------|:---------:|:------:|
| 1 | POST /auth/login → JWT токен | P0 | ⬜ |
| 2 | GET /portfolio → позиции + P&L | P0 | ⬜ |
| 3 | GET /quotes?figis=... → котировки | P0 | ⬜ |
| 4 | POST /orders → создать ордер | P0 | ⬜ |
| 5 | DELETE /orders/{id} → отменить | P1 | ⬜ |
| 6 | POST /strategies → создать SMA | P1 | ⬜ |
| 7 | POST /strategies/{id}/start → запустить | P1 | ⬜ |
| 8 | Web UI работает | P1 | ⬜ |

### Архитектурные требования

| # | Требование | Статус |
|---|------------|:------:|
| 1 | Гексагональная архитектура | ⬜ |
| 2 | Ports & Adapters | ⬜ |
| 3 | Boost.DI | ⬜ |
| 4 | PostgreSQL + миграции | ⬜ |
| 5 | fake-jwt-server | ⬜ |
| 6 | SimpleBrokerGatewayAdapter | ⬜ |
| 7 | InMemoryEventBus | ⬜ |
| 8 | Prometheus метрики | ⬜ |
| 9 | Docker Compose | ⬜ |

---

# 📚 Этап 3: Education (22-28 декабря)

## Цель

Демонстрация микросервисных паттернов: разделение на сервисы, API Gateway, RabbitMQ.

## План по дням

### День 1 (пн 22.12): Подготовка + ZITADEL

```
□ Скопировать mvp → education
□ Добавить ZITADEL в docker-compose
□ Настроить Realm + Application
□ Создать тестовых пользователей
□ Заменить FakeJwtAdapter на ZitadelAdapter
```

### День 2 (вт 23.12): Service Decomposition

```
□ Выделить Market Service:
  └─ IMarketService → отдельный сервис
  └─ Своя PostgreSQL БД (market_db)
  └─ Dockerfile + docker-compose
□ Выделить Trading Service:
  └─ IOrderService + IPortfolioService
  └─ Своя PostgreSQL БД (trading_db)
```

### День 3 (ср 24.12): API Gateway

```
□ Создать API Gateway сервис:
  └─ JWT validation
  └─ Routing по URL path
  └─ X-Trace-ID propagation
  └─ Rate limiting (базовый)
□ Обновить nginx конфигурацию
```

### День 4 (чт 25.12): RabbitMQ

```
□ Добавить RabbitMQ в docker-compose
□ Создать RabbitMqEventBus:
  └─ Exchange: trading.events (topic)
  └─ Queues для каждого сервиса
□ Заменить InMemoryEventBus → RabbitMqEventBus
□ Протестировать события между сервисами
```

### День 5 (пт 26.12): REST между сервисами

```
□ HttpClient для inter-service communication
□ Market Service ← Trading Service
□ Circuit Breaker (базовый)
□ Retry policy
```

### День 6 (сб 27.12): Тестирование + Документация

```
□ Integration tests
□ demo.sh для Education
□ README.md
□ C4 диаграммы (компоненты)
```

### День 7 (вс 28.12): Финализация

```
□ Исправление багов
□ Финальная проверка
□ Подготовка к видео
```

---

## Education Checklist

### Микросервисные паттерны

| Паттерн | Статус |
|---------|:------:|
| Service Decomposition (3 сервиса) | ⬜ |
| API Gateway | ⬜ |
| Database per Service | ⬜ |
| Event-Driven (RabbitMQ) | ⬜ |
| Distributed Tracing (X-Trace-ID) | ⬜ |
| Service Discovery (Docker DNS) | ⬜ |
| Circuit Breaker | ⬜ |

FIXME: добавить обязательно мониторинг (Prometeus, может получится Graphana, если по времени успеем)!!!
Продумать как будут работать метрики, определить какие метрики собираем.

### Инфраструктура

| Компонент | Статус |
|-----------|:------:|
| ZITADEL | ⬜ |
| RabbitMQ | ⬜ |
| 3 PostgreSQL databases | ⬜ |
| Docker Compose (multi-service) | ⬜ |

---

# 🎬 Этап 4: Видео-защита (29.12 - 3.01)

## План

```
29.12 (пн)  → Запись видео (7-10 минут)
              ├─ Demo MVP
              ├─ Demo Education
              ├─ Объяснение архитектуры
              └─ Отправка друзьям на review

30.12-2.01  → Правки если нужно

3.01 (пт)   → Финальная версия
              → Сдача проекта
```

## Структура видео

```
0:00 - 1:00   Введение, цель проекта
1:00 - 3:00   Демо MVP (curl + UI)
3:00 - 5:00   Демо Education (микросервисы)
5:00 - 7:00   Архитектура (C4 диаграммы)
7:00 - 8:00   Паттерны и принятые решения
8:00 - 10:00  Q&A, заключение
```

---

# 🔧 Production (после курса)

## Что добавим

```
□ Реальный Tinkoff Invest API (gRPC)
□ Kubernetes deployment
□ Собственный Auth Service
□ Дополнительные стратегии (RSI, MACD)
□ Бэктестинг
□ WebSocket для real-time котировок
□ Grafana dashboards
□ CI/CD pipeline
```

---

# 🚨 Fallback стратегия

## Если не успеваем MVP к 21.12

```
❌ Пропускаем:
  • SMA стратегия (делаем после)
  • Web UI (только curl)
  • Unit тесты

✅ Обязательно:
  • REST endpoints работают
  • PostgreSQL с данными
  • Docker Compose запускается
  • SimpleBrokerGatewayAdapter
  • fake-jwt-server
```

## Если не успеваем Education к 28.12

```
❌ Пропускаем:
  • RabbitMQ (оставляем InMemory)
  • ZITADEL (оставляем fake-jwt-server)
  • Circuit Breaker

✅ Обязательно:
  • 2-3 отдельных сервиса
  • API Gateway
  • Docker Compose multi-service
```

---

# 📋 Ежедневный трекинг

## Формат ежедневного отчёта

```markdown
## День N (дата)

### Сделано
- [ ] Задача 1
- [ ] Задача 2

### Проблемы
- Проблема 1 → Решение

### План на завтра
- Задача 1
- Задача 2
```

---

# 🎯 Критерии успеха

## MVP Success Criteria

```
✅ docker compose up -d  → все контейнеры running
✅ curl /health         → 200 OK
✅ curl /auth/login     → JWT токен
✅ curl /portfolio      → позиции
✅ curl /quotes         → котировки
✅ curl POST /orders    → ордер создан
✅ Web UI              → страница открывается
```

## Education Success Criteria

```
✅ 3+ Docker контейнера (сервисы)
✅ API Gateway работает
✅ RabbitMQ передаёт события
✅ Каждый сервис своя БД
✅ X-Trace-ID проходит через все сервисы
```

# Дополнитьельные требования от преподавателя
1) В части архитектуры рекомендую С4 model, контейнерную диаграмму или деплоймент.
С примерами можно ознакомиться здесь:
https://github.com/team7katas/sysopsquad?tab=readme-ov-file#process-views
https://c4model.com/diagrams/deployment

2) Сценарии для показа - ОК, нужно реализовывать и демонстрировать. Также рекомендую добавить сиквенс-диаграмму для ключевого сценария(возможно увидим откат распределенных транзакций, оркестрацию/хореографию и тд).

3) Доменные сущности - ОК. Однако и выделенные микросервисы у вас должны быть.

4) Напоминаю, что на выходе должен быть работающий проект с использованием микросервисной архитектуры, метрики, использованные паттерны. Как проходит защита можно посмотреть в занятиях в группе, студенты из вашей группы уже защищались.

P.S. Надо подумать в итоге над демонстрацией, что мы показываем.
Обязательно нужно большое покрытие unit-тестами бизнес-логики.
Обязательно нужны тесты endpoint-ов с документацией.
Может сэкономить время на ручной торговле, а запустить демонстрацию бесконечной автоматической торговли.

Пока я вижу так: в итоге есть некоторый один скрипт demo.sh, который поднимает в docker-compose все микросервисы, запускает автоматическую торговлю.
В видео я показываю как работают endpoint-ы, потом показываю дашборд с автоматическо  торговлей, потом смотрим метрики (может графану подключим, если успеем), потом смотрим trace-id (тут желательно использовать рабочую технологию для cpp, поискать в интернете).

Потом отдельно руками или через другой скрипт проверяем краш-тест системы:
отказ какого-то сревиса - как поведет себя система;
БД отвалилась - легко воспроизвести, просто не запускать докер-контейнер;
... другие случаи, откат транзакции распеделенной и т.п.
Может это реализовать в тестах интеграционных, чтоб сильно не раздувать выступление, скорее всего не успею показать (или показать что-то одно, а про остальные рассказать как запустить, отметить факт, что реализованы).
---

# Что дополнительно доработать
## Общие замечания к архитектуре (разделение hpp/cpp)
Сейчас много логики в include папке. Так как выявлется много багов, то временно можно жить в такой парадигме, легче править.
Но ближе к концу проекта надо в hpp-файле оставить только интерфейсы, темплейты и хэдеры с подробным документированием и описанием, а всю логику перести по возможности в src.

## Отчет с уровнем покрытия
Разобраться с багом, почему уровень покрытия 0, хотя тесты есть и проходят.
Возможно проблема в том, что тест лежит не в папаке tests, а во внутренней папке.

## Рабочие метрики
Необходимо изучить как подключить Prometeus/Graphana.
Как собирать метрики.
Реализовать подготовительные классы.



---

*Roadmap v5.1*  
*Дата: 26 декабря 2025*
