# k8s/trading-postgres.yaml
# ConfigMap trading-postgres-init генерируется Kustomize из trading-service/sql/
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: trading-postgres-pvc
  namespace: trading
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: trading-postgres
  namespace: trading
spec:
  replicas: 1
  selector:
    matchLabels:
      app: trading-postgres
  template:
    metadata:
      labels:
        app: trading-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:15
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              valueFrom:
                secretKeyRef:
                  name: trading-secrets
                  key: TRADING_DB_NAME
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: trading-secrets
                  key: TRADING_DB_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: trading-secrets
                  key: TRADING_DB_PASSWORD
          volumeMounts:
            - name: postgres-storage
              mountPath: /var/lib/postgresql/data
            - name: init-sql
              mountPath: /docker-entrypoint-initdb.d
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "trading_user", "-d", "trading_db"]
            initialDelaySeconds: 5
            periodSeconds: 5
      volumes:
        - name: postgres-storage
          persistentVolumeClaim:
            claimName: trading-postgres-pvc
        - name: init-sql
          configMap:
            name: trading-postgres-init
---
apiVersion: v1
kind: Service
metadata:
  name: trading-postgres
  namespace: trading
spec:
  selector:
    app: trading-postgres
  ports:
    - port: 5432
      targetPort: 5432
