# Trading Platform Microservices

> **Курсовой проект:** OTUS Microservice Architecture  
> **Автор:** Антон Тоболкин  
> **Дата:** Январь 2025

Платформа для алгоритмической торговли на Московской бирже. Реализована как набор микросервисов с event-driven архитектурой.

---

## 📋 Содержание

1. [Быстрый старт (для проверяющего)](#-быстрый-старт-для-проверяющего)
2. [Архитектура](#-архитектура)
3. [C4 Диаграммы](#-c4-диаграммы)
4. [Микросервисы](#-микросервисы)
5. [Аутентификация](#-аутентификация)
6. [Event-Driven Architecture](#-event-driven-architecture)
7. [Распределённые транзакции](#-распределённые-транзакции)
8. [Паттерны](#-паттерны)
9. [База данных](#-база-данных)
10. [API Reference](#-api-reference)
11. [E2E тестовые сценарии](#-e2e-тестовые-сценарии)
12. [Мониторинг](#-мониторинг)
13. [Разработка](#-разработка)
14. [Технологии](#-технологии)

---

## 🚀 Быстрый старт (для проверяющего)

### Требования

- Kubernetes (minikube / k3s / Docker Desktop)
- kubectl
- Настроенный Ingress с доменом `arch.homework`

### Развёртывание (образы уже в DockerHub)

```bash
# 1. Клонировать репозиторий
git clone https://github.com/Anton/trading-platform.git
cd trading-platform/education

# 2. Применить манифесты
kubectl apply -f k8s/

# 3. Проверить статус (подождать 1-2 минуты)
kubectl get pods -n trading

# 4. Проверить health endpoints
curl http://arch.homework/auth/health
curl http://arch.homework/trading/health
curl http://arch.homework/broker/health
```

### Запуск E2E тестов

```bash
# Установить Newman (если нет)
npm install -g newman

# Запустить тесты
newman run postman/trading-platform-e2e.json
```

### Доступ к сервисам

| Сервис | URL |
|--------|-----|
| Auth API | http://arch.homework/auth |
| Trading API | http://arch.homework/trading |
| Broker API | http://arch.homework/broker |
| RabbitMQ UI | http://arch.homework/rabbitmq |
| Prometheus | http://arch.homework/prometheus |
| Grafana | http://arch.homework/grafana |

---

## 🏗 Архитектура

### Общая схема

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              TRADING PLATFORM                               │
└─────────────────────────────────────────────────────────────────────────────┘

                                   Internet
                                      │
                                      ▼
                           ┌──────────────────┐
                           │     Ingress      │
                           │  arch.homework   │
                           └────────┬─────────┘
                                    │
            ┌───────────────────────┼───────────────────────┐
            │                       │                       │
            ▼                       ▼                       ▼
   ┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
   │  Auth Service   │    │ Trading Service │    │ Broker Service  │
   │     :8081       │    │     :8082       │    │     :8083       │
   │                 │    │   (API Gateway) │    │ (Source of Truth)│
   └────────┬────────┘    └────────┬────────┘    └────────┬────────┘
            │                      │                      │
            │              ┌───────┴───────┐              │
            │              │               │              │
            │         REST │          AMQP │              │
            │              ▼               ▼              │
            │    ┌──────────────┐  ┌──────────────┐       │
            │    │ Auth Service │  │   RabbitMQ   │◀──────┤
            │    │  (validate)  │  │trading.events│       │
            │    └──────────────┘  └──────────────┘       │
            │                                             │
            ▼                       ▼                     ▼
   ┌─────────────────┐    ┌─────────────────┐   ┌─────────────────┐
   │    auth_db      │    │   trading_db    │   │   broker_db     │
   │   PostgreSQL    │    │   PostgreSQL    │   │   PostgreSQL    │
   └─────────────────┘    └─────────────────┘   └─────────────────┘
```

### Ключевые решения

| Решение | Обоснование |
|---------|-------------|
| **Broker = Source of Truth** | Все данные о балансах, ордерах, позициях в одном месте |
| **Trading = API Gateway** | Валидация, кэширование, idempotency |
| **Единый Exchange** | `trading.events` для всех событий (упрощение, один routing) |
| **Event-Driven для команд** | Асинхронная обработка ордеров через RabbitMQ |
| **REST для запросов** | Синхронное чтение данных напрямую |

---

## 🗺 C4 Диаграммы

### Level 1: System Context

```
┌─────────────────────────────────────────────────────────────────┐
│                     SYSTEM CONTEXT                              │
└─────────────────────────────────────────────────────────────────┘

    ┌─────────┐
    │ Trader  │
    │ (User)  │
    └────┬────┘
         │ HTTPS/REST
         ▼
┌─────────────────────┐
│  Trading Platform   │
│                     │
│ Платформа для       │
│ алгоритмической     │
│ торговли            │
└─────────┬───────────┘
          │ gRPC/REST (будущее)
          ▼
┌─────────────────────┐         ┌─────────────────┐
│ Tinkoff Invest API  │────────▶│ Moscow Exchange │
│ (External Broker)   │   FIX   │     (MOEX)      │
└─────────────────────┘         └─────────────────┘
```

### Level 2: Container Diagram

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           TRADING PLATFORM                                  │
└─────────────────────────────────────────────────────────────────────────────┘

                              ┌─────────┐
                              │ Trader  │
                              └────┬────┘
                                   │ HTTPS
                                   ▼
                           ┌──────────────┐
                           │   Ingress    │
                           │    nginx     │
                           └──────┬───────┘
                                  │
            ┌─────────────────────┼─────────────────────┐
            │ /auth               │ /trading            │ /broker
            ▼                     ▼                     ▼
   ┌─────────────────┐   ┌─────────────────┐   ┌─────────────────┐
   │  Auth Service   │◀──│ Trading Service │──▶│ Broker Service  │
   │    C++:8081     │   │    C++:8082     │   │    C++:8083     │
   │                 │   │  (API Gateway)  │   │(Source of Truth)│
   └────────┬────────┘   └────────┬────────┘   └────────┬────────┘
            │                     │                     │
            │              ┌──────┴──────┐              │
            │              │             │              │
            │         HTTP │        AMQP │              │
            │              ▼             ▼              │
            │    ┌──────────────────────────────┐       │
            │    │         RabbitMQ             │◀──────┤
            │    │      trading.events          │       │
            │    │                              │       │
            │    │ order.create ───────────────▶│       │
            │    │ order.cancel ───────────────▶│       │
            │    │◀─────────── order.filled     │       │
            │    │◀─────────── order.rejected   │       │
            │    │◀─────────── portfolio.updated│       │
            │    └──────────────────────────────┘       │
            │                                           │
            ▼                     ▼                     ▼
   ┌─────────────────┐   ┌─────────────────┐   ┌─────────────────┐
   │    auth_db      │   │   trading_db    │   │   broker_db     │
   │   PostgreSQL    │   │   PostgreSQL    │   │   PostgreSQL    │
   │                 │   │                 │   │                 │
   │ • users         │   │ • idempotency_  │   │ • broker_orders │
   │ • accounts      │   │   keys          │   │ • broker_positions│
   │ • sessions      │   │                 │   │ • broker_balances│
   └─────────────────┘   └─────────────────┘   │ • instruments   │
                                               │ • quotes        │
                                               └─────────────────┘
```

### Level 3: Component Diagram — Trading Service

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          TRADING SERVICE (API Gateway)                      │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│  PRIMARY ADAPTERS (HTTP Handlers)                                           │
│                                                                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐    │
│  │HealthHandler │  │MarketHandler │  │PortfolioHndlr│  │IdempotentHndlr│   │
│  │ GET /health  │  │GET /instruments│ │GET /portfolio│  │  (Decorator) │   │
│  └──────────────┘  │GET /quotes   │  └──────┬───────┘  └──────┬───────┘   │
│                    └──────┬───────┘         │                  │           │
│                           │                 │                  ▼           │
│                           │                 │          ┌──────────────┐    │
│                           │                 │          │ OrderHandler │    │
│                           │                 │          │POST/DELETE   │    │
│                           │                 │          │  /orders     │    │
│                           │                 │          └──────┬───────┘    │
└───────────────────────────┼─────────────────┼─────────────────┼────────────┘
                            │                 │                 │
                            ▼                 ▼                 ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  APPLICATION LAYER                                                          │
│                                                                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐    │
│  │MarketService │  │PortfolioSvc  │  │ OrderService │  │TradingEvent  │    │
│  │              │  │              │  │              │  │  Handler     │    │
│  │• getInstruments│ │• getPortfolio│ │• createOrder │  │• onOrderFilled│   │
│  │• getQuotes   │  │• getPositions│  │• cancelOrder │  │• onPortfolio │    │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘  │  Updated     │    │
│         │                 │                 │          └──────┬───────┘    │
└─────────┼─────────────────┼─────────────────┼─────────────────┼────────────┘
          │                 │                 │                 │
          ▼                 ▼                 ▼                 ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  SECONDARY ADAPTERS                                                         │
│                                                                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐    │
│  │HttpAuthClient│  │CachedBroker  │  │RabbitMQ      │  │PostgresIdem- │    │
│  │              │  │  Gateway     │  │  Adapter     │  │ potencyRepo  │    │
│  │POST /validate│  │• LRU Cache   │  │• Publisher   │  │• find(key)   │    │
│  └──────┬───────┘  │• HttpBroker  │  │• Consumer    │  │• save(key)   │    │
│         │          └──────┬───────┘  └──────┬───────┘  └──────┬───────┘    │
└─────────┼─────────────────┼─────────────────┼─────────────────┼────────────┘
          │                 │                 │                 │
          ▼                 ▼                 ▼                 ▼
   ┌────────────┐   ┌────────────┐   ┌────────────┐   ┌────────────┐
   │Auth Service│   │Broker Svc  │   │  RabbitMQ  │   │trading_db  │
   │  :8081     │   │  :8083     │   │            │   │ PostgreSQL │
   └────────────┘   └────────────┘   └────────────┘   └────────────┘
```

### Sequence: Create Order (Idempotent)

```
  Client          IdempotentHandler        trading_db          OrderHandler
    │                    │                     │                    │
    │ POST /orders       │                     │                    │
    │ X-Idempotency-Key: │                     │                    │
    │   "key-123"        │                     │                    │
    │───────────────────▶│                     │                    │
    │                    │                     │                    │
    │                    │ SELECT FROM         │                    │
    │                    │ idempotency_keys    │                    │
    │                    │────────────────────▶│                    │
    │                    │                     │                    │
    │                    │ NOT FOUND           │                    │
    │                    │◀────────────────────│                    │
    │                    │                     │                    │
    │                    │ delegate            │                    │
    │                    │────────────────────────────────────────▶│
    │                    │                     │                    │
    │                    │                     │    201 {order_id}  │
    │                    │◀────────────────────────────────────────│
    │                    │                     │                    │
    │                    │ INSERT INTO         │                    │
    │                    │ idempotency_keys    │                    │
    │                    │────────────────────▶│                    │
    │                    │                     │                    │
    │ 201 {order_id}     │                     │                    │
    │◀───────────────────│                     │                    │
    │                    │                     │                    │
    │ POST /orders       │                     │                    │
    │ X-Idempotency-Key: │                     │                    │
    │   "key-123" (same) │                     │                    │
    │───────────────────▶│                     │                    │
    │                    │                     │                    │
    │                    │ SELECT FROM         │                    │
    │                    │ idempotency_keys    │                    │
    │                    │────────────────────▶│                    │
    │                    │                     │                    │
    │                    │ FOUND {cached}      │                    │
    │                    │◀────────────────────│                    │
    │                    │                     │                    │
    │ 201 {same order_id}│ ╔═══════════════════════════════════╗   │
    │ X-Idempotency-Key- │ ║ CACHE HIT — handler NOT called    ║   │
    │   Used: true       │ ╚═══════════════════════════════════╝   │
    │◀───────────────────│                     │                    │
```

---

## 🔧 Микросервисы

### Auth Service (порт 8081)

**Ответственность:** Аутентификация, авторизация, управление аккаунтами.

```
┌─────────────────────────────────────────────────────────────────┐
│                        AUTH SERVICE                             │
├─────────────────────────────────────────────────────────────────┤
│  Adapters (Primary)           │  Adapters (Secondary)           │
│  ─────────────────            │  ──────────────────             │
│  • RegisterHandler            │  • PostgresUserRepository       │
│  • LoginHandler               │  • PostgresAccountRepository    │
│  • LogoutHandler              │  • PostgresSessionRepository    │
│  • ValidateTokenHandler       │  • FakeJwtProvider              │
│  • GetAccessTokenHandler      │                                 │
│  • GetAccountsHandler         │                                 │
│  • AddAccountHandler          │                                 │
├─────────────────────────────────────────────────────────────────┤
│  Application                  │  Domain                         │
│  ───────────                  │  ──────                         │
│  • AuthService                │  • User                         │
│  • AccountService             │  • Account (SANDBOX/REAL)       │
│                               │  • Session                      │
└─────────────────────────────────────────────────────────────────┘
```

**Эндпоинты:**

| Метод | Путь | Описание | Auth |
|-------|------|----------|------|
| GET | `/health` | Health check | - |
| POST | `/api/v1/auth/register` | Регистрация | - |
| POST | `/api/v1/auth/login` | Логин → session_token | - |
| POST | `/api/v1/auth/logout` | Выход | Session |
| POST | `/api/v1/auth/validate` | Валидация токена (internal) | - |
| POST | `/api/v1/auth/access-token` | Получить access_token | Session |
| GET | `/api/v1/accounts` | Список аккаунтов | Session |
| POST | `/api/v1/accounts` | Создать аккаунт | Session |
| DELETE | `/api/v1/accounts/{id}` | Удалить аккаунт | Session |

---

### Trading Service (порт 8082)

**Ответственность:** API Gateway, валидация, кэширование, idempotency, публикация команд.

```
┌─────────────────────────────────────────────────────────────────┐
│                       TRADING SERVICE                           │
├─────────────────────────────────────────────────────────────────┤
│  Adapters (Primary)           │  Adapters (Secondary)           │
│  ─────────────────            │  ──────────────────             │
│  • OrderHandler               │  • HttpAuthClient               │
│  • PortfolioHandler           │  • HttpBrokerGateway            │
│  • MarketHandler              │  • CachedBrokerGateway          │
│  • IdempotentHandler          │  • RabbitMQPublisher            │
│                               │  • RabbitMQConsumer             │
│                               │  • PostgresIdempotencyRepo      │
├─────────────────────────────────────────────────────────────────┤
│  Application                  │  Domain                         │
│  ───────────                  │  ──────                         │
│  • OrderService               │  • Order, OrderRequest          │
│  • MarketService              │  • Portfolio, Position          │
│  • PortfolioService           │  • Quote, Instrument            │
│  • TradingEventHandler        │  • Money                        │
└─────────────────────────────────────────────────────────────────┘
```

**Эндпоинты:**

| Метод | Путь | Описание | Auth |
|-------|------|----------|------|
| GET | `/health` | Health check | - |
| GET | `/api/v1/instruments` | Список инструментов | - |
| GET | `/api/v1/instruments/{figi}` | Инструмент по FIGI | - |
| GET | `/api/v1/instruments/search?query=` | Поиск | - |
| GET | `/api/v1/quotes?figis=` | Котировки | - |
| POST | `/api/v1/orders` | Создать ордер → RabbitMQ | Access |
| GET | `/api/v1/orders` | Список ордеров | Access |
| GET | `/api/v1/orders/{id}` | Ордер по ID | Access |
| DELETE | `/api/v1/orders/{id}` | Отменить ордер → RabbitMQ | Access |
| GET | `/api/v1/portfolio` | Портфель | Access |

**Кэширование:**

| Данные | TTL | Размер | Обоснование |
|--------|-----|--------|-------------|
| Котировки | Короткий | 1000 | Часто меняются |
| Инструменты | Длинный | 500 | Редко меняются |

---

### Broker Service (порт 8083)

**Ответственность:** Исполнение ордеров, управление балансом, симуляция биржи.

```
┌─────────────────────────────────────────────────────────────────┐
│                       BROKER SERVICE                            │
├─────────────────────────────────────────────────────────────────┤
│  Adapters (Primary)           │  Adapters (Secondary)           │
│  ─────────────────            │  ──────────────────             │
│  • OrdersHandler (GET only)   │  • FakeBrokerAdapter            │
│  • PortfolioHandler           │  • PostgresBrokerOrderRepo      │
│  • InstrumentsHandler         │  • PostgresBrokerPositionRepo   │
│  • QuotesHandler              │  • PostgresBrokerBalanceRepo    │
│                               │  • RabbitMQPublisher            │
│                               │  • RabbitMQConsumer             │
├─────────────────────────────────────────────────────────────────┤
│  Application                  │  Domain                         │
│  ───────────                  │  ──────                         │
│  • OrderCommandHandler        │  • BrokerOrder                  │
│  • MarketDataPublisher        │  • BrokerPosition               │
│                               │  • BrokerBalance                │
│  Broker Simulation            │  • Instrument, Quote            │
│  ──────────────────           │                                 │
│  • OrderProcessor             │  Events                         │
│  • PriceSimulator             │  ──────                         │
│  • MarketScenario             │  • OrderCreatedEvent            │
│                               │  • OrderFilledEvent             │
│                               │  • OrderRejectedEvent           │
└─────────────────────────────────────────────────────────────────┘
```

**Эндпоинты (Internal API):**

| Метод | Путь | Описание |
|-------|------|----------|
| GET | `/health` | Health check |
| GET | `/api/v1/orders?account_id=` | Ордера аккаунта |
| GET | `/api/v1/orders/{id}?account_id=` | Ордер по ID |
| GET | `/api/v1/portfolio?account_id=` | Портфель |
| GET | `/api/v1/instruments` | Список инструментов |
| GET | `/api/v1/instruments/{figi}` | Инструмент по FIGI |
| GET | `/api/v1/quotes?figi=` | Котировка |

**Режимы симуляции (BROKER_FILL_BEHAVIOR):**

| Режим | Описание |
|-------|----------|
| `IMMEDIATE` | Мгновенное исполнение |
| `REALISTIC` | Market сразу, Limit ждут цену |
| `PARTIAL` | Частичное исполнение |
| `DELAYED` | С задержкой (async) |
| `ALWAYS_REJECT` | Всегда отклонять (для тестов) |

---

## 🔐 Аутентификация

### Двухуровневая система токенов

```
┌─────────────────────────────────────────────────────────────────┐
│                    AUTHENTICATION FLOW                          │
└─────────────────────────────────────────────────────────────────┘

  Client                     Auth Service                Trading Service
    │                             │                            │
    │ 1. POST /auth/register      │                            │
    │────────────────────────────▶│                            │
    │         {user_id}           │                            │
    │◀────────────────────────────│                            │
    │                             │                            │
    │ 2. POST /auth/login         │                            │
    │────────────────────────────▶│                            │
    │      {session_token}        │                            │
    │◀────────────────────────────│                            │
    │                             │                            │
    │ 3. GET /accounts            │                            │
    │    [Session Token]          │                            │
    │────────────────────────────▶│                            │
    │      [{account_id, ...}]    │                            │
    │◀────────────────────────────│                            │
    │                             │                            │
    │ 4. POST /auth/access-token  │                            │
    │    {account_id}             │                            │
    │    [Session Token]          │                            │
    │────────────────────────────▶│                            │
    │      {access_token}         │                            │
    │◀────────────────────────────│                            │
    │                             │                            │
    │ 5. POST /orders             │                            │
    │    [Access Token]           │                            │
    │─────────────────────────────┼───────────────────────────▶│
    │                             │   POST /auth/validate      │
    │                             │◀────────────────────────────│
    │                             │   {user_id, account_id}    │
    │                             │────────────────────────────▶│
    │         {order_id}          │                            │
    │◀────────────────────────────┼────────────────────────────│
```

### Типы токенов

| Token | Содержит | Назначение | TTL |
|-------|----------|------------|-----|
| `session_token` | user_id | Управление в Auth Service | 24 часа |
| `access_token` | user_id + account_id | Операции в Trading Service | 1 час |

### Примеры запросов

```bash
# 1. Регистрация
curl -X POST http://arch.homework/auth/api/v1/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username": "trader1", "email": "trader@example.com", "password": "secret123"}'

# 2. Логин → session_token
curl -X POST http://arch.homework/auth/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username": "trader1", "password": "secret123"}'
# Response: {"session_token": "eyJ...", "user_id": "user-xxx"}

# 3. Получить access_token
curl -X POST http://arch.homework/auth/api/v1/auth/access-token \
  -H "Authorization: Bearer <session_token>" \
  -H "Content-Type: application/json" \
  -d '{"account_id": "acc-sandbox-001"}'
# Response: {"access_token": "eyJ...", "account_id": "acc-sandbox-001"}

# 4. Использовать access_token
curl http://arch.homework/trading/api/v1/orders \
  -H "Authorization: Bearer <access_token>"
```

---

## 📨 Event-Driven Architecture

### Обзор

```
┌─────────────────────────────────────────────────────────────────┐
│                      EVENT ARCHITECTURE                         │
└─────────────────────────────────────────────────────────────────┘

  Trading Service              RabbitMQ                 Broker Service
       │                          │                           │
       │                          │                           │
       │    order.create ────────▶│                           │
       │    order.cancel ────────▶│  trading.events ─────────▶│
       │                          │    (exchange)             │
       │                          │                           │
       │◀──────── order.created ──│                           │
       │◀──────── order.filled ───│◀───────────────────────────│
       │◀─ order.partially_filled │                           │
       │◀──────── order.rejected ─│                           │
       │◀──────── order.cancelled │                           │
       │◀──────── quote.updated ──│                           │
       │◀───── portfolio.updated ─│                           │
```

### Почему один Exchange?

| Вариант | Плюсы | Минусы |
|---------|-------|--------|
| **Два exchange** (trading.events + broker.events) | Логическое разделение | Сложнее конфигурация |
| **Один exchange** (trading.events) ✅ | Простота, единый routing | Все события в одном месте |

**Решение:** Используем один `trading.events` exchange типа `topic`. Routing keys определяют направление.

### События

| Событие | Producer | Consumer | Описание |
|---------|----------|----------|----------|
| `order.create` | Trading | Broker | Команда на создание ордера |
| `order.cancel` | Trading | Broker | Команда на отмену ордера |
| `order.created` | Broker | Trading | Ордер принят в обработку |
| `order.filled` | Broker | Trading | Ордер полностью исполнен |
| `order.partially_filled` | Broker | Trading | Частичное исполнение |
| `order.rejected` | Broker | Trading | Ордер отклонён |
| `order.cancelled` | Broker | Trading | Ордер отменён |
| `quote.updated` | Broker | Trading | Котировка обновлена |
| `portfolio.updated` | Broker | Trading | Портфель обновлён |

### Формат событий

**order.create:**
```json
{
  "order_id": "ord-a1b2c3d4",
  "account_id": "acc-001",
  "figi": "BBG004730N88",
  "direction": "BUY",
  "type": "MARKET",
  "quantity": 10,
  "price": 265.50,
  "currency": "RUB",
  "timestamp": "2025-01-04T12:00:00Z"
}
```

**order.filled:**
```json
{
  "order_id": "ord-a1b2c3d4",
  "account_id": "acc-001",
  "figi": "BBG004730N88",
  "status": "FILLED",
  "executed_lots": 10,
  "executed_price": 265.75,
  "currency": "RUB",
  "timestamp": 1704369601000
}
```

**quote.updated:**
```json
{
  "figi": "BBG004730N88",
  "bid": 265.50,
  "ask": 265.80,
  "last_price": 265.65,
  "currency": "RUB",
  "timestamp": 1704369600000
}
```

**portfolio.updated:**
```json
{
  "account_id": "acc-001",
  "timestamp": 1704369601000,
  "cash": {"amount": 973500.00, "currency": "RUB"},
  "positions": [
    {"figi": "BBG004730N88", "quantity": 100, "average_price": 265.75}
  ],
  "total_value": {"amount": 1000100.00, "currency": "RUB"}
}
```

### Сценарий: Успешный Market Order

```
┌─────────────────────────────────────────────────────────────────┐
│                 SCENARIO: SUCCESSFUL ORDER                      │
└─────────────────────────────────────────────────────────────────┘

  Client              Trading                Broker               DB
    │                    │                     │                   │
    │ POST /orders       │                     │                   │
    │ {figi, qty, BUY}   │                     │                   │
    │───────────────────▶│                     │                   │
    │                    │                     │                   │
    │                    │ Validate JWT        │                   │
    │                    │ Validate FIGI       │                   │
    │                    │ Check Idempotency   │                   │
    │                    │                     │                   │
    │                    │ order.create ──────▶│                   │
    │                    │                     │                   │
    │ 201 {PENDING}      │                     │ Check balance     │
    │◀───────────────────│                     │──────────────────▶│
    │                    │                     │ Reserve funds     │
    │                    │                     │──────────────────▶│
    │                    │                     │                   │
    │                    │                     │ Execute order     │
    │                    │                     │──────────────────▶│
    │                    │                     │                   │
    │                    │ order.filled ◀──────│ Update position   │
    │                    │◀────────────────────│──────────────────▶│
    │                    │                     │                   │
    │                    │ portfolio.updated ◀─│ Commit reserved   │
    │                    │◀────────────────────│──────────────────▶│
    │                    │                     │                   │
    │ GET /orders/{id}   │                     │                   │
    │───────────────────▶│                     │                   │
    │                    │ GET /orders ───────▶│                   │
    │                    │◀────────────────────│                   │
    │ 200 {FILLED}       │                     │                   │
    │◀───────────────────│                     │                   │
```

### Сценарий: Отклонённый Order (Insufficient Funds)

```
┌─────────────────────────────────────────────────────────────────┐
│              SCENARIO: REJECTED ORDER (NO FUNDS)                │
└─────────────────────────────────────────────────────────────────┘

  Client              Trading                Broker               DB
    │                    │                     │                   │
    │ POST /orders       │                     │                   │
    │ {figi, qty, BUY}   │                     │                   │
    │───────────────────▶│                     │                   │
    │                    │                     │                   │
    │                    │ order.create ──────▶│                   │
    │                    │                     │                   │
    │ 201 {PENDING}      │                     │ Check balance     │
    │◀───────────────────│                     │──────────────────▶│
    │                    │                     │                   │
    │                    │                     │ ❌ Insufficient   │
    │                    │                     │    funds          │
    │                    │                     │                   │
    │                    │ order.rejected ◀────│                   │
    │                    │ {reason: "..."}     │                   │
    │                    │◀────────────────────│                   │
    │                    │                     │                   │
    │ GET /orders/{id}   │                     │                   │
    │───────────────────▶│                     │                   │
    │ 200 {REJECTED}     │                     │                   │
    │◀───────────────────│                     │                   │
```

### Сценарий: Частичное исполнение

```
┌─────────────────────────────────────────────────────────────────┐
│              SCENARIO: PARTIAL FILL                             │
└─────────────────────────────────────────────────────────────────┘

  Client              Trading                Broker               DB
    │                    │                     │                   │
    │ POST /orders       │                     │                   │
    │ {qty: 100}         │                     │                   │
    │───────────────────▶│                     │                   │
    │                    │ order.create ──────▶│                   │
    │ 201 {PENDING}      │                     │                   │
    │◀───────────────────│                     │                   │
    │                    │                     │ Execute 50/100    │
    │                    │                     │──────────────────▶│
    │                    │                     │                   │
    │                    │ order.partially_    │                   │
    │                    │ filled {50}    ◀────│                   │
    │                    │◀────────────────────│                   │
    │                    │                     │                   │
    │                    │                     │ (позже) 50/100    │
    │                    │                     │──────────────────▶│
    │                    │                     │                   │
    │                    │ order.filled ◀──────│                   │
    │                    │◀────────────────────│                   │
    │                    │                     │                   │
    │ GET /orders/{id}   │                     │                   │
    │───────────────────▶│                     │                   │
    │ 200 {FILLED,       │                     │                   │
    │      qty: 100}     │                     │                   │
    │◀───────────────────│                     │                   │
```

---

## 🔄 Распределённые транзакции

### Архитектурное решение: Избежание Saga

```
┌─────────────────────────────────────────────────────────────────┐
│            ПОЧЕМУ У НАС НЕТ РАСПРЕДЕЛЁННЫХ ТРАНЗАКЦИЙ           │
└─────────────────────────────────────────────────────────────────┘

  ❌ Альтернатива (сложная):           ✅ Наш подход (простой):
  
  Trading           Broker             Trading           Broker
     │                 │                  │                 │
     │ INSERT order    │                  │                 │
     │────▶ trading_db │                  │ (stateless*)   │
     │                 │                  │                 │
     │ order.create ──▶│                  │ order.create ─▶│
     │                 │                  │                 │
     │ order.filled ◀──│                  │ order.filled ◀─│ ← ВСЯ логика
     │                 │                  │                 │   в одном месте
     │ UPDATE order    │                  │                 │
     │────▶ trading_db │                  │                 │
     │                 │                  │                 │
     ⚠️ Что если упал   │                  ✅ Нет проблем   │
        между шагами?  │                     синхронизации │

  * Trading хранит только idempotency_keys, не бизнес-данные
```

### Принцип: Single Source of Truth

| Сущность | Владелец | Обоснование |
|----------|----------|-------------|
| Order | Broker Service | Один владелец = нет распределённой транзакции |
| Balance | Broker Service | Резервирование + списание атомарно |
| Position | Broker Service | Обновление атомарно с ордером |
| Idempotency | Trading Service | Защита от дублей на уровне API Gateway |
| User | Auth Service | Изолированный bounded context |
| Account | Auth Service | Связан с пользователем |

### Вместо Saga: Fail-Fast

```
┌─────────────────────────────────────────────────────────────────┐
│                    FAIL-FAST VALIDATION                         │
└─────────────────────────────────────────────────────────────────┘

                        Broker Service
                              │
    order.create ────────────▶│
                              │
                              ▼
                    ┌─────────────────┐
                    │ 1. Check balance│──── Недостаточно? → REJECTED
                    └────────┬────────┘
                             │ OK
                             ▼
                    ┌─────────────────┐
                    │ 2. Check position│──── Нет позиции? → REJECTED
                    │    (для SELL)    │
                    └────────┬────────┘
                             │ OK
                             ▼
                    ┌─────────────────┐
                    │ 3. Reserve funds │
                    │ 4. Execute order │
                    │ 5. Update position│
                    │ 6. Commit/Release │
                    └─────────────────┘
                              │
                              ▼
                    order.filled / order.rejected
```

**Ключевое отличие:** Мы проверяем ВСЕ условия ДО начала операции, а не откатываем ПОСЛЕ ошибки.

### Trade-offs

| Решение | Плюсы | Минусы |
|---------|-------|--------|
| Order только в Broker | Нет распределённых транзакций | Trading не работает автономно |
| Trading = Stateless | Легко масштабировать | При обрыве связи с Broker — UI не работает |
| Fail-fast валидация | Нет откатов, быстрый ответ | Меньше гибкости |

---

## 🎯 Паттерны

### Архитектурные паттерны микросервисов

| Паттерн | Где реализован | Описание |
|---------|----------------|----------|
| **Microservices** | Весь проект | 3 независимых сервиса с отдельными БД |
| **API Gateway** | Trading Service | Единая точка входа, аутентификация, маршрутизация |
| **Database per Service** | auth_db, trading_db, broker_db | Изоляция данных, независимое развёртывание |
| **Event-Driven** | RabbitMQ | Асинхронное взаимодействие через события |
| **CQRS** | Trading + Broker | Команды через события, запросы через HTTP |

### Структурные паттерны

| Паттерн | Где реализован | Описание |
|---------|----------------|----------|
| **Hexagonal Architecture** | Все сервисы | Ports & Adapters, инверсия зависимостей |
| **Dependency Injection** | Boost.DI | Конфигурация зависимостей в App классах |
| **Repository** | Postgres*Repository | Абстракция доступа к данным |
| **Proxy** | CachedBrokerGateway | Кэширование поверх HttpBrokerGateway |

### Паттерны надёжности

| Паттерн | Где реализован | Описание |
|---------|----------------|----------|
| **Idempotent Consumer** | IdempotentHandler | Защита от повторной обработки запросов |
| **Fail-Fast** | OrderCommandHandler | Проверка условий до начала операции |
| **Correlation ID** | order_id в событиях | Отслеживание запроса через систему |

### Idempotency (Идемпотентность)

```
┌─────────────────────────────────────────────────────────────────┐
│                    IDEMPOTENCY PATTERN                          │
└─────────────────────────────────────────────────────────────────┘

  Client                  IdempotentHandler              OrderHandler
    │                           │                             │
    │ POST /orders              │                             │
    │ X-Idempotency-Key: abc123 │                             │
    │──────────────────────────▶│                             │
    │                           │                             │
    │                           │ Check DB: key exists?       │
    │                           │────────────────────────────▶│
    │                           │ NO                          │
    │                           │◀────────────────────────────│
    │                           │                             │
    │                           │ Execute ───────────────────▶│
    │                           │                             │
    │                           │ Save: key → {status, body}  │
    │                           │────────────────────────────▶│
    │                           │                             │
    │ 201 {order_id: xxx}       │                             │
    │◀──────────────────────────│                             │
    │                           │                             │
    │ POST /orders (RETRY)      │                             │
    │ X-Idempotency-Key: abc123 │                             │
    │──────────────────────────▶│                             │
    │                           │                             │
    │                           │ Check DB: key exists?       │
    │                           │────────────────────────────▶│
    │                           │ YES → cached response       │
    │                           │◀────────────────────────────│
    │                           │                             │
    │ 201 {order_id: xxx}       │ (same response, no execute) │
    │◀──────────────────────────│                             │
```

**Реализация:** Декоратор `IdempotentHandler` оборачивает `OrderHandler`:
- Проверяет заголовок `X-Idempotency-Key`
- Кэширует успешные ответы (2xx) в таблицу `idempotency_keys`
- При повторном запросе возвращает кэшированный ответ

---

## 🗄 База данных

### Auth Service (auth_db)

```sql
-- Пользователи
CREATE TABLE users (
    user_id VARCHAR(64) PRIMARY KEY,
    username VARCHAR(64) UNIQUE NOT NULL,
    email VARCHAR(128) UNIQUE NOT NULL,
    password_hash VARCHAR(256) NOT NULL,
    created_at TIMESTAMP DEFAULT NOW()
);

-- Брокерские аккаунты
CREATE TABLE accounts (
    account_id VARCHAR(64) PRIMARY KEY,
    user_id VARCHAR(64) REFERENCES users(user_id),
    name VARCHAR(64) NOT NULL,
    type VARCHAR(16) NOT NULL,  -- SANDBOX / REAL
    tinkoff_token_encrypted TEXT,
    created_at TIMESTAMP DEFAULT NOW()
);

-- Сессии
CREATE TABLE sessions (
    session_id VARCHAR(64) PRIMARY KEY,
    user_id VARCHAR(64) REFERENCES users(user_id),
    jwt_token VARCHAR(512) NOT NULL,
    expires_at TIMESTAMP NOT NULL,
    created_at TIMESTAMP DEFAULT NOW()
);
```

### Trading Service (trading_db)

```sql
-- Идемпотентность запросов
CREATE TABLE idempotency_keys (
    key VARCHAR(64) PRIMARY KEY,
    response_status INTEGER NOT NULL,
    response_body TEXT,
    created_at TIMESTAMP DEFAULT NOW()
);
```

### Broker Service (broker_db)

```sql
-- Инструменты
CREATE TABLE instruments (
    figi VARCHAR(12) PRIMARY KEY,
    ticker VARCHAR(10) NOT NULL,
    name VARCHAR(256) NOT NULL,
    currency VARCHAR(3) DEFAULT 'RUB',
    lot_size INTEGER DEFAULT 1,
    min_price_increment INTEGER DEFAULT 100
);

-- Котировки
CREATE TABLE quotes (
    figi VARCHAR(12) PRIMARY KEY REFERENCES instruments(figi),
    bid BIGINT NOT NULL,
    ask BIGINT NOT NULL,
    last_price BIGINT NOT NULL,
    updated_at TIMESTAMP DEFAULT NOW()
);

-- Ордера
CREATE TABLE broker_orders (
    order_id VARCHAR(64) PRIMARY KEY,
    account_id VARCHAR(64) NOT NULL,
    figi VARCHAR(12) NOT NULL,
    direction VARCHAR(4) NOT NULL,      -- BUY / SELL
    quantity INTEGER NOT NULL,
    filled_quantity INTEGER DEFAULT 0,
    price BIGINT NOT NULL,              -- в копейках
    order_type VARCHAR(8) NOT NULL,     -- MARKET / LIMIT
    status VARCHAR(20) NOT NULL,        -- PENDING / FILLED / PARTIALLY_FILLED / REJECTED / CANCELLED
    reject_reason VARCHAR(256),
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);

-- Позиции
CREATE TABLE broker_positions (
    account_id VARCHAR(64) NOT NULL,
    figi VARCHAR(12) NOT NULL,
    quantity INTEGER NOT NULL,
    avg_price BIGINT NOT NULL,          -- в копейках
    updated_at TIMESTAMP DEFAULT NOW(),
    PRIMARY KEY(account_id, figi)
);

-- Балансы
CREATE TABLE broker_balances (
    account_id VARCHAR(64) PRIMARY KEY,
    currency VARCHAR(3) NOT NULL DEFAULT 'RUB',
    available BIGINT NOT NULL DEFAULT 0,  -- доступно (копейки)
    reserved BIGINT NOT NULL DEFAULT 0,   -- зарезервировано под ордера (копейки)
    updated_at TIMESTAMP DEFAULT NOW()
);
```

### Резервирование баланса

```
┌─────────────────────────────────────────────────────────────────┐
│                   BALANCE RESERVATION FLOW                      │
└─────────────────────────────────────────────────────────────────┘

Начало: available = 1,000,000₽, reserved = 0₽

1. BUY 10 лотов SBER @ 265₽
   cost = 10 × 10 × 265 = 26,500₽
   
   reserve(26,500):
   ├── available = 1,000,000 - 26,500 = 973,500₽
   └── reserved  = 0 + 26,500 = 26,500₽

2a. FILLED:
    commitReserved(26,500):
    └── reserved = 26,500 - 26,500 = 0₽
    (деньги ушли на покупку акций)

2b. REJECTED / CANCELLED:
    releaseReserved(26,500):
    ├── reserved  = 26,500 - 26,500 = 0₽
    └── available = 973,500 + 26,500 = 1,000,000₽
    (деньги вернулись)

2c. PARTIALLY_FILLED (50%):
    filled_cost = 5 × 10 × 265 = 13,250₽
    commitReserved(13,250):
    └── reserved = 26,500 - 13,250 = 13,250₽
    (остаток ждёт исполнения)
```

---

## 📡 API Reference

### Создание ордера

```http
POST /trading/api/v1/orders
Authorization: Bearer <access_token>
Content-Type: application/json
X-Idempotency-Key: unique-key-123

{
  "figi": "BBG004730N88",
  "quantity": 10,
  "direction": "BUY",
  "type": "MARKET"
}
```

**Response (201):**
```json
{
  "order_id": "ord-abc123",
  "status": "PENDING",
  "message": "Order submitted for processing"
}
```

### Limit Order

```http
POST /trading/api/v1/orders
Authorization: Bearer <access_token>
Content-Type: application/json

{
  "figi": "BBG004730N88",
  "quantity": 10,
  "direction": "BUY",
  "type": "LIMIT",
  "price": 265.50
}
```

**Примечание:** `price` обязателен для LIMIT, игнорируется для MARKET.

### Получение статуса ордера

```http
GET /trading/api/v1/orders/ord-abc123
Authorization: Bearer <access_token>
```

**Response (200):**
```json
{
  "order_id": "ord-abc123",
  "figi": "BBG004730N88",
  "direction": "BUY",
  "type": "MARKET",
  "quantity": 10,
  "filled_quantity": 10,
  "status": "FILLED",
  "executed_price": 265.75,
  "created_at": "2025-01-04T12:00:00Z"
}
```

### Получение портфолио

```http
GET /trading/api/v1/portfolio
Authorization: Bearer <access_token>
```

**Response (200):**
```json
{
  "account_id": "acc-sandbox-001",
  "cash": {
    "amount": 973500.00,
    "currency": "RUB"
  },
  "positions": [
    {
      "figi": "BBG004730N88",
      "ticker": "SBER",
      "quantity": 100,
      "average_price": 265.75,
      "current_price": 268.00,
      "expected_yield": 225.00
    }
  ],
  "total_value": 1000300.00
}
```

### Коды ошибок

| Код | Описание | Пример |
|-----|----------|--------|
| 400 | Невалидный запрос | Missing FIGI, quantity <= 0 |
| 401 | Не авторизован | Нет токена или токен невалиден |
| 404 | Не найдено | Ордер не найден |
| 500 | Внутренняя ошибка | Database connection failed |

---

## 🧪 E2E тестовые сценарии

### Обзор

| # | Сценарий | Assertions | Статус |
|---|----------|------------|--------|
| 0 | Setup - Authentication | 8 | ✅ |
| 1 | Initial State - Portfolio | 3 | ✅ |
| 2 | Market Order Flow (Async) | 8 | ✅ |
| 3 | Limit Order + Cancel | 6 | ✅ |
| 4 | Negative Tests | 5 | ✅ |
| 5 | Final State - All Orders | 4 | ✅ |
| 6 | Idempotency Tests | 10 | ✅ |
| **Total** | | **48** | ✅ |

### 1. Health Checks

```javascript
// Test: All services healthy
pm.test('Auth service healthy', () => {
    pm.response.to.have.status(200);
});
// Repeat for Trading, Broker
```

### 2. Auth Flow

```javascript
// Step 1: Register
// Step 2: Login → session_token
// Step 3: Get accounts
// Step 4: Get access_token
// Step 5: Validate access_token
```

### 3. Market Data

```javascript
// Get instruments
pm.test('Has instruments', () => {
    pm.expect(pm.response.json().length).to.be.above(0);
});

// Get quotes
pm.test('Quote has bid/ask', () => {
    var json = pm.response.json();
    pm.expect(json.bid).to.be.above(0);
    pm.expect(json.ask).to.be.above(0);
});
```

### 4. Market Order Success (Async)

```javascript
// Step 1: POST /orders → 201 {PENDING}
pm.test('Order accepted', () => {
    pm.response.to.have.status(201);
    pm.expect(pm.response.json().status).to.eql('PENDING');
});

// Step 2: Wait 1 second (pm.setTimeout or Pre-request delay)

// Step 3: GET /orders/{id} → {FILLED}
pm.test('Order filled via RabbitMQ', () => {
    pm.expect(pm.response.json().status).to.eql('FILLED');
});

// Step 4: GET /portfolio → position exists
pm.test('Position added', () => {
    var positions = pm.response.json().positions;
    pm.expect(positions.length).to.be.above(0);
});
```

### 5. Limit Order + Cancel

```javascript
// Step 1: POST /orders {type: LIMIT, price: 200} → PENDING
// Step 2: DELETE /orders/{id} → 200
// Step 3: GET /orders/{id} → {CANCELLED}
```

### 6. Insufficient Funds

```javascript
// POST /orders {quantity: 1000000} → PENDING
// Wait
// GET /orders/{id} → {REJECTED, reason: "Insufficient funds"}
```

### 7. Invalid Token

```javascript
// POST /orders without Authorization → 401
// POST /orders with invalid token → 401
```

### 8. Idempotency

```javascript
// Step 1: POST /orders with X-Idempotency-Key: key1 → 201 {order_id: xxx}
// Step 2: POST /orders with X-Idempotency-Key: key1 (same) → 201 {order_id: xxx}
//         ✅ Same order_id returned
//         ✅ Header X-Idempotency-Key-Used: true
// Step 3: POST /orders with X-Idempotency-Key: key2 (different) → 201 {order_id: yyy}
//         ✅ Different order_id (new order created)
// Step 4: POST /orders without X-Idempotency-Key → 201
//         ✅ Works without key (key is optional)
```

---

## 📊 Мониторинг

### Эндпоинты

| Сервис | Health | Metrics |
|--------|--------|---------|
| Auth | `/health` | `/metrics` |
| Trading | `/health` | `/metrics` |
| Broker | `/health` | `/metrics` |

### Метрики (Prometheus format)

```
# HTTP метрики
http_requests_total{service="trading",method="POST",path="/api/v1/orders",status="201"} 150
http_requests_total{service="trading",method="GET",path="/api/v1/orders",status="200"} 1200

# Бизнес метрики
orders_total{direction="BUY",status="FILLED"} 142
orders_total{direction="SELL",status="REJECTED"} 8

# RabbitMQ метрики
rabbitmq_events_published_total{routing_key="order.create"} 150
rabbitmq_events_consumed_total{routing_key="order.filled"} 142
```

---

## 🛠 Разработка

### Требования

- CMake 3.16+
- GCC 12+ / Clang 14+
- Docker & Docker Compose
- Kubernetes + kubectl
- Make

### Сборка

```bash
# Из корня проекта
mkdir build && cd build
cmake -DBUILD_TESTS=ON ..
make -j$(nproc)

# Запуск unit-тестов
ctest --output-on-failure
```

### Makefile команды (для разработчика)

| Команда | Описание |
|---------|----------|
| `make build` | Собрать Docker образы |
| `make push` | Пушнуть в Docker Hub |
| `make deploy` | Применить k8s манифесты |
| `make ship` | build + push + redeploy всех сервисов |
| `make ship-auth` | Только auth-service |
| `make ship-trading` | Только trading-service |
| `make ship-broker` | Только broker-service |
| `make test-e2e` | E2E тесты (Postman/Newman) |
| `make test-e2e-debug` | E2E + логи при падении |
| `make reset-dbs` | Сброс БД к начальному состоянию |
| `make logs` | Логи всех сервисов |
| `make status` | Статус подов |

### Переменные окружения

**Auth Service:**

| Переменная | Default | Описание |
|------------|---------|----------|
| `HTTP_PORT` | 8081 | Порт сервера |
| `AUTH_DB_HOST` | localhost | Хост PostgreSQL |
| `AUTH_DB_PORT` | 5432 | Порт PostgreSQL |
| `AUTH_DB_NAME` | auth_db | Имя БД |
| `AUTH_DB_USER` | auth_user | Пользователь |
| `AUTH_DB_PASSWORD` | - | Пароль (обязательно) |

**Trading Service:**

| Переменная | Default | Описание |
|------------|---------|----------|
| `HTTP_PORT` | 8082 | Порт сервера |
| `AUTH_SERVICE_HOST` | auth-service | Хост Auth |
| `AUTH_SERVICE_PORT` | 8081 | Порт Auth |
| `BROKER_SERVICE_HOST` | broker-service | Хост Broker |
| `BROKER_SERVICE_PORT` | 8083 | Порт Broker |
| `RABBITMQ_HOST` | rabbitmq | Хост RabbitMQ |
| `RABBITMQ_PORT` | 5672 | Порт RabbitMQ |
| `TRADING_DB_HOST` | trading-postgres | Хост PostgreSQL |
| `TRADING_DB_PORT` | 5432 | Порт PostgreSQL |
| `TRADING_DB_NAME` | trading_db | Имя БД |

**Broker Service:**

| Переменная | Default | Описание |
|------------|---------|----------|
| `HTTP_PORT` | 8083 | Порт сервера |
| `DB_HOST` | localhost | Хост PostgreSQL |
| `DB_PORT` | 5432 | Порт PostgreSQL |
| `DB_NAME` | broker_db | Имя БД |
| `RABBITMQ_HOST` | rabbitmq | Хост RabbitMQ |
| `BROKER_FILL_BEHAVIOR` | REALISTIC | Режим симуляции |

---

## 🔧 Технологии

### Собственные библиотеки

| Библиотека | Описание |
|------------|----------|
| [cpp-http-server](https://github.com/...) | HTTP сервер на Boost.Beast + Boost.DI |
| [cpp-cache](https://github.com/...) | Thread-safe LRU кэш на чистом C++ |

### Backend

| Технология | Версия | Назначение |
|------------|--------|------------|
| C++ | 17/20 | Язык разработки |
| Boost.Beast | 1.83 | HTTP Server |
| Boost.DI | 1.3 | Dependency Injection |
| Boost.Asio | 1.83 | Async I/O |
| nlohmann/json | 3.11 | JSON парсинг |
| AMQP-CPP | 4.3 | RabbitMQ клиент |
| libpqxx | 7.9 | PostgreSQL клиент |
| GoogleTest | 1.14 | Unit тесты |

### Инфраструктура

| Технология | Назначение |
|------------|------------|
| Docker | Контейнеризация |
| Kubernetes | Оркестрация |
| Kustomize | Конфигурация k8s |
| RabbitMQ | Message Broker |
| PostgreSQL | База данных |
| nginx Ingress | Reverse Proxy |
| Prometheus | Метрики |
| Grafana | Визуализация |

### CI/CD

| Инструмент | Назначение |
|------------|------------|
| GitHub Actions | CI pipeline |
| gcovr | Coverage report |

---

## 📁 Структура проекта

```
education/
├── auth-service/
│   ├── include/
│   │   ├── adapters/primary/      # HTTP handlers
│   │   ├── adapters/secondary/    # PostgreSQL, JWT
│   │   ├── application/           # AuthService, AccountService
│   │   ├── domain/                # User, Account, Session
│   │   └── ports/                 # Interfaces
│   ├── src/
│   ├── tests/
│   ├── sql/
│   ├── Dockerfile
│   └── CMakeLists.txt
│
├── trading-service/
│   ├── include/
│   │   ├── adapters/primary/      # OrderHandler, MarketHandler, IdempotentHandler
│   │   ├── adapters/secondary/    # HttpBrokerGateway, RabbitMQ, PostgresIdempotencyRepo
│   │   ├── application/           # OrderService, TradingEventHandler
│   │   ├── domain/                # Order, Portfolio, Quote
│   │   └── ports/                 # IBrokerGateway, IEventPublisher, IIdempotencyRepository
│   ├── src/
│   ├── tests/
│   ├── sql/
│   ├── Dockerfile
│   └── CMakeLists.txt
│
├── broker-service/
│   ├── include/
│   │   ├── adapters/primary/      # OrdersHandler, PortfolioHandler
│   │   ├── adapters/secondary/    # FakeBrokerAdapter, PostgreSQL
│   │   │   └── broker/            # OrderProcessor, PriceSimulator
│   │   ├── application/           # OrderCommandHandler
│   │   ├── domain/                # BrokerOrder, BrokerBalance
│   │   └── ports/
│   ├── src/
│   ├── tests/
│   ├── sql/
│   ├── Dockerfile
│   └── CMakeLists.txt
│
├── k8s/
│   ├── namespace.yaml
│   ├── secret.yaml
│   ├── auth-postgres.yaml
│   ├── auth-service.yaml
│   ├── trading-postgres.yaml
│   ├── trading-service.yaml
│   ├── broker-postgres.yaml
│   ├── broker-service.yaml
│   ├── rabbitmq.yaml
│   └── ingress.yaml
│
├── postman/
│   └── trading-platform-e2e.json
│
├── Makefile
├── CMakeLists.txt
├── README.md
└── TODO.md
```

---
