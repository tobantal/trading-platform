# Чеклист ручного тестирования Trading Platform MVP

## ✅ Подготовка
- [ ] Docker и Docker Compose установлены
- [ ] `docker-compose up -d` выполнен успешно
- [ ] Все контейнеры запущены (`docker-compose ps`)
- [ ] Логи без критических ошибок (`docker-compose logs --tail=50`)

## ✅ 1. Health Check
- [ ] `GET /api/v1/health` возвращает 200 OK
- [ ] JSON содержит статус "ok"
- [ ] В `services` есть все сервисы
- [ ] timestamp соответствует текущему времени

## ✅ 2. Аутентификация (Auth)

### Логин
- [ ] `POST /auth/login` с пустым username → 400 Bad Request
- [ ] `POST /auth/login` с username="trader1" → 200 OK + JWT токен
- [ ] `POST /auth/login` с username="newuser" → 200 OK (создаёт нового пользователя)
- [ ] Токен содержит поля: `access_token`, `token_type`, `expires_in`

### Валидация токена
- [ ] `POST /auth/validate` с валидным токеном → `{"valid": true}`
- [ ] В ответе есть `user_id` и `username`
- [ ] `POST /auth/validate` с невалидным токеном → `{"valid": false}`
- [ ] `POST /auth/validate` без токена → 400 Bad Request

## ✅ 3. Рыночные данные (Market)

### Котировки
- [ ] `GET /quotes` без параметров → все 5 инструментов
- [ ] `GET /quotes?figis=BBG004730N88` → только SBER
- [ ] `GET /quotes?figis=BBG004730N88,BBG004731032` → два инструмента
- [ ] Каждая котировка содержит: `figi`, `ticker`, `last_price`, `bid_price`, `ask_price`, `currency`, `updated_at`

### Инструменты
- [ ] `GET /instruments` → 5 инструментов
- [ ] `GET /instruments/search?query=SBER` → найден SBER
- [ ] `GET /instruments/search?query=ЛУКОЙЛ` → найден LKOH
- [ ] `GET /instruments/search?query=INVALID` → пустой массив
- [ ] `GET /instruments/BBG004730N88` → информация по SBER
- [ ] `GET /instruments/INVALID` → 404 Not Found

## ✅ 4. Ордера (Orders)

### Создание ордера (требуется Authorization: Bearer)
- [ ] Без токена → 401 Unauthorized
- [ ] С неверным токеном → 401 Unauthorized
- [ ] MARKET BUY SBER quantity=10 → 201 Created
- [ ] MARKET SELL SBER quantity=10 → 201 Created (если есть позиция)
- [ ] LIMIT BUY с ценой → 201 Created, статус PENDING
- [ ] С quantity=0 → 400 Bad Request
- [ ] С несуществующим FIGI → 400 Bad Request

### Получение ордеров
- [ ] `GET /orders` → список всех ордеров счёта
- [ ] `GET /orders/{id}` → информация по конкретному ордеру
- [ ] `GET /orders/INVALID` → 404 Not Found

### Отмена ордера
- [ ] `DELETE /orders/{id}` (PENDING ордер) → 200 OK
- [ ] `DELETE /orders/{id}` (FILLED ордер) → 400 Bad Request
- [ ] `DELETE /orders/INVALID` → 400 Bad Request

## ✅ 5. Портфель (Portfolio)

### Полный портфель
- [ ] `GET /portfolio` → `account_id`, `cash`, `total_value`, `positions`
- [ ] `cash` равен 1 000 000 ₽ при первом запросе
- [ ] После покупки акций `cash` уменьшается
- [ ] `total_value` корректно рассчитывается

### Позиции
- [ ] `GET /portfolio/positions` → массив позиций
- [ ] После покупки SBER → появляется позиция
- [ ] Позиция содержит: `figi`, `ticker`, `quantity`, `average_price`, `current_price`, `pnl`

### Денежные средства
- [ ] `GET /portfolio/cash` → `{"available": 1000000.0, "currency": "RUB"}`

## ✅ 6. Стратегии (Strategies)

### Создание стратегии
- [ ] Без name → 400 Bad Request
- [ ] Без figi → 400 Bad Request
- [ ] С валидными параметрами → 201 Created
- [ ] В ответе есть `id`, `name`, `type`, `status`

### Управление стратегиями
- [ ] `GET /strategies` → список стратегий
- [ ] `GET /strategies/{id}` → информация по стратегии
- [ ] `POST /strategies/{id}/start` → 200 OK, статус RUNNING
- [ ] `POST /strategies/{id}/stop` → 200 OK, статус STOPPED
- [ ] `DELETE /strategies/{id}` (RUNNING) → 400 Bad Request
- [ ] `DELETE /strategies/{id}` (STOPPED) → 200 OK

## ✅ 7. Мониторинг

### Health endpoint
- [ ] `GET /health` всегда доступен
- [ ] Все сервисы отображаются корректно

### Prometheus metrics
- [ ] `GET /metrics` → текст в формате Prometheus
- [ ] Метрики содержат: `http_requests_total`, `orders_placed_total`, `cache_hits_total`

## ✅ 8. Интеграционные тесты

### Последовательность действий
1. [ ] Логин пользователя → получаем токен
2. [ ] Получить котировки SBER → проверяем цену
3. [ ] Купить 10 лотов SBER по MARKET → проверяем исполнение
4. [ ] Проверить портфель → появилась позиция, cash уменьшился
5. [ ] Создать стратегию SMA для SBER → успешно
6. [ ] Запустить стратегию → статус RUNNING
7. [ ] Остановить стратегию → статус STOPPED
8. [ ] Продать 5 лотов SBER → частичное закрытие позиции

### Проверка состояния
- [ ] После всех операций health check показывает "ok"
- [ ] Метрики увеличиваются корректно
- [ ] Логи не содержат ошибок
- [ ] Все endpoint-ы отвечают в пределах 1 секунды

## ✅ 9. Граничные случаи

### Негативные сценарии
- [ ] Отправка некорректного JSON → 400 Bad Request
- [ ] Несуществующий endpoint → 404 Not Found
- [ ] Неподдерживаемый HTTP метод → 404 Not Found
- [ ] SQL injection в query параметрах → безопасно обрабатывается
- [ ] XSS в username → безопасно обрабатывается

### Предельные значения
- [ ] Количество лотов = 0 → 400 Bad Request
- [ ] Очень большое количество лотов → проверка доступности средств
- [ ] Негативная цена → 400 Bad Request
- [ ] Слишком длинные строки в параметрах → безопасно обрезаются

## 📝 Результаты тестирования

**Дата тестирования:** _______________

**Тестировщик:** ____________________

**Результат:** 
- [ ] Все проверки пройдены
- [ ] Критические баги: 0
- [ ] Некритические баги: _____
- [ ] Замечания: ___________________________________

**Статус системы:** 
- [ ] Готово к использованию
- [ ] Требуются доработки
- [ ] Требуется повторное тестирование

----